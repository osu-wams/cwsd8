langcode: en
status: true
dependencies: {  }
id: profile
label: Profile
routes:
  -
    route_name: entity.node.edit_form
    route_params:
      node: osu_profile
tips:
  test:
    id: test
    plugin: text
    label: test
    weight: -100
    position: auto
    selector:
    body: '<p>asdfasdfadsf</p>'
  managing_nodes:
    id: managing_nodes
    plugin: text
    label: 'Managing Nodes'
    weight: -99
    position: auto
    selector:
    body: '<p>Edit the content of a page that are below the main menu and above the footer. You can also create drafts and Schedule Publishing.</p>'
  node_operations:
    id: node_operations
    plugin: text
    label: 'Node Operations'
    weight: -98
    position: auto-start
    selector: '#block-madrone-local-tasks'
    body: '<p>Depending on the permissions you have access to you can Edit the page and its metadata. Configure the layout override, view revisions or Delete.</p>'
  edit:
    id: edit
    plugin: text
    label: Edit
    weight: -97
    position: bottom
    selector: '.nav-tabs:nth-child(2)'
    body: "<p>If you want a basic page with just text and maybe a photo or two, you can use the edit tab. However, if you want more complex layout, you'll want to use the Layout tab.</p>\r\n<p>Think of the edit tab more as settings for the page, where you can set the path/alias, add it to a menu, or change the metadata information.</p> "
  workflows:
    id: workflows
    plugin: text
    label: Workflows
    weight: -96
    position: bottom
    selector: 'nav#toolbar-bar > div.moderation-sidebar-toolbar-tab'
    body: '<p>Here you can see if there is a draft available and even create a new draft.</p>'
